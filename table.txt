  CREATE TABLE newsnow (
    author VARCHAR ,
    title VARCHAR NOT NULL ,
    description TEXT,
    url TEXT ,
    urlToImage TEXT ,
    publishedAt DATE ,
    source VARCHAR,
    PRIMARY KEY (url ,source )

  );


INSERT INTO newsnow (author,title,url,urlToImage,source,description)VALUES ('cseng','site under construction','https://www.facebook.com/jasonveng','www','world','Haa ?')


  CREATE TABLE weather(
    cityid INTEGER ,
    name VARCHAR ,
    country VARCHAR ,
    dt INTEGER ,
    temp DECIMAL,
    description VARCHAR ,
    icon VARCHAR ,
    wind DECIMAL,
    dt_text DATE,
    PRIMARY KEY (cityid, dt)
  );
